#ifndef EventTime_H
#define EventTime_H

typedef struct EventTime EventTime;

struct EventTime
{
    // EventTime *left;
    // EventTime *right;
    // char colour;    //'b' or 'r' or 'd' (black or red or double black)
    long long time;
};

#endif // EventTime_H